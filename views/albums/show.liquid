{% layout "layout.liquid" %} {% block content %}
<div>
    <h2>{{ album.title }}</h2>
    <img src="{{ album.cover_big }}" alt="{{ album.title }}"><br>
    {% if summary %}
    <p>{{ summary }}</p>
    {% endif %}
    
</div>
{% endblock %}