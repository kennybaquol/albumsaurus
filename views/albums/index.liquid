{% layout "layout.liquid" %} {% block content %}
<div>
    <h2>Discover Albums</h2>

    <section>
        {% for album in data limit: 3 %}
        <a href="/albums/{{ album.id }}" name="name" class="item">

            <img src="{{ album.cover_medium}}" alt="{{ album.title }}"><br>
            <!-- <h4>{{ album.title }}</h4> -->
        </a>
        {% else %}
        The collection is empty.
        {% endfor %}
    </section>
    <h5>{{ artistName }}</h5>

</div>
{% endblock %}